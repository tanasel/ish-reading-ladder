(function () {
  "use strict";

  // No endpoints, no keys, no analytics. Everything runs in the browser.
  // basePath is only used if the site is served from a sub-folder.
  window.EAL_CONFIG = {
    basePath: "",
    appName: "Reading Ladder",
    org: "International School of The Hague",
  };
})();
